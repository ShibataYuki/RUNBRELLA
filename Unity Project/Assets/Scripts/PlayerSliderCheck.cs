using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerSliderCheck : MonoBehaviour
{
    
    //BoxCollider2D boxCollider;
    //Ray2D ray;
    //public RaycastHit2D hit;
    //Player player;
    //[SerializeField]
    //LayerMask layerMask;

    //// Start is called before the first frame update
    //void Start()
    //{
    //    boxCollider = GetComponent<BoxCollider2D>();
    //    player = GetComponent<Player>();
    //}
    
    //public RaycastHit2D RayHitCheck()
    //{
    //    Vector2 playerTopPos;
    //    Vector2 playerBottomPos;
    //    float rayLength;
    //    playerTopPos    = new Vector2(transform.position.x, transform.position.y + (boxCollider.size.y / 2) + boxCollider.offset.y);
    //    playerBottomPos = new Vector2(transform.position.x, transform.position.y - (boxCollider.size.y / 2) + boxCollider.offset.y);
    //    rayLength =  playerTopPos.y - playerBottomPos.y;
    //    hit = Physics2D.Raycast(playerTopPos, -(transform.up), rayLength, layerMask);
    //    return hit;
    //}

}
